new updates require customtk.

''pip install customtkinter'' in terminal
then run open the folder with all the python files, ( do not touch), right click and do "open terminal" or similar, then type "python main.py"
